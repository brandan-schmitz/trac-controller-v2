name: trac_controller_v2
description: "The next-gen control waterpark control system for the Two Rivers Activity Center"
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ^3.9.2
dependencies:
  flutter:
    sdk: flutter
  window_manager: ^0.5.1
  shared_preferences: ^2.5.3
  mqtt_client: ^10.11.1
  yaml: ^3.1.3
  process_run: ^1.2.4
  path: ^1.9.1
  collection: ^1.19.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
